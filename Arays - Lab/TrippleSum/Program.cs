using System;
using System.Linq;

namespace TrippleSum
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] numbers = Console.ReadLine().Split(' ').Select(int.Parse).ToArray();

            for (int i = 0; i < numbers[1]; i++)
            {
                for (int j = numbers[]; j < numbers.Length; j++)
                {
                   
                }
            }
        }
    }
}
